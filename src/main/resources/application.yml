
---

spring:
  application:
    name: stage-service
  config:
    activate:
      on-profile: local
    import: "optional:configserver:http://localhost:8888/"

---

spring:
  application:
    name: stage-service
  config:
    activate:
      on-profile: stage
    import: "optional:configserver:http://172.17.0.2:8888/"
